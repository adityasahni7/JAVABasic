package arrays;

public class arrays15 {
    int numbers[] = {1,2,3,4,5};
    int copyOfnumbers[] = Arrays.copyOf(numbers. numbers.length);
    System.out.println(Arrays.toString(numbers, copyOfNumbers));
}
